[user]
    name = Michael Noronha
    email = michaeltnoronha@gmail.com

[alias]
    st = status
    co = commit -m
    ca = commit --amend
    al = add -u
    aa = add -A
    cl = clone
    rh = reset --hard
    ch = checkout
    chp = cherry-pick
    p = pull
    pu = push
    l = log --pretty=oneline --abbrev-commit --graph --decorate
    d = diff
    dc = diff --cached
; TODO allow a commit has to be passed in
    fi = diff-tree --no-commit-id --name-only -r HEAD
[core]
    editor = vim
	excludesfile = /Users/michaelnoronha/.gitignore
[github]
	user = mtn
